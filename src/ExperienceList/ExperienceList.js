import React    from "react";
import template from "./ExperienceList.jsx";

class ExperienceList extends React.Component {
  render() {
    return template.call(this);
  }
}

export default ExperienceList;
